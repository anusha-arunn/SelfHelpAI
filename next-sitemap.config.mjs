//eslint-disable-next-linr no-undef
/** @type {import('next-sitemap').IConfig} */
const config = {
  siteUrl: "https://aitherapist.co",
  generateRobotsTxt: true, // (optional)
  // ...other options
};

export default config;
